import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.roktobondhu.app',
  appName: 'Roktobondhu Bangladesh',
  webDir: 'dist'
};

export default config;
